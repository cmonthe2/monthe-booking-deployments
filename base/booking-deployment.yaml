apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-api
  template:
    metadata:
      labels:
        app: booking-api
    spec:
      nodeSelector:
        kubernetes.io/os: linux
      containers:
      - name: booking-api
        image: 123456789012.dkr.ecr.us-east-1.amazonaws.com/booking-api:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
spec:
  type: NodePort
  selector:
    app: booking-api
  ports:
  - port: 80
    targetPort: 8080
